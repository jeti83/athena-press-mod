package com.hypixel.hytale.entity;

import java.util.UUID;

/** Stub – wird durch die echte Hytale-Spieler-Referenz ersetzt sobald die API verfügbar ist. */
public interface PlayerRef {
    UUID getUuid();
    String getUsername();
}
